{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd $VERCEL_PROJECT_ROOT/apps/docs && npm install && npm run build && cd $VERCEL_PROJECT_ROOT/apps/evaluation-tool && npm install && npm run build",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/evaluate/:path*",
      "destination": "/apps/evaluation-tool/dist/:path*"
    },
    {
      "source": "/:path*",
      "destination": "/apps/docs/build/:path*"
    }
  ],
  "outputDirectory": ".",
  "framework": null
}
