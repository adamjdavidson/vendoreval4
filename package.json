{
  "name": "vendoreval-monorepo",
  "version": "0.2.0",
  "private": true,
  "description": "VendorEval unified deployment - docs site and evaluation tool",
  "scripts": {
    "build:all": "npm run build:docs && npm run build:tool",
    "build:docs": "cd apps/docs && npm install && npm run build",
    "build:tool": "cd apps/evaluation-tool && npm install && npm run build",
    "dev:docs": "cd apps/docs && npm run start",
    "dev:tool": "cd apps/evaluation-tool && npm run dev"
  },
  "workspaces": [
    "apps/docs",
    "apps/evaluation-tool"
  ]
}
